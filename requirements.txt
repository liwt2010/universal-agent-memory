# Core dependencies
# No strict required dependencies - UAMS is designed to work with minimal imports
# Install optional extras based on your use case

# Production storage backends
# sqlite3 is built into Python - no extra install needed
# redis>=5.0.0
# neo4j>=5.0.0
# psycopg2-binary>=2.9.0
# chromadb>=0.4.0

# Embedding
# tiktoken>=0.7.0
# sentence-transformers>=2.0.0

# Dev dependencies (install with: pip install -r requirements-dev.txt)
